{
  "name": "angular-dynamic-layout",
  "version": "0.0.1",
  "description": "A simple library to be able to click and drag widgets inside a container and to place them dynamically. The proportion are kept through resize.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "angular",
    "grid",
    "dynamic",
    "layout"
  ],
  "author": "Pierre Reliquet <pierre.reliquet@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "angular": "1.3.15"
  }
}
